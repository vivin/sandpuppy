#!/bin/bash

if [ $# -lt 2 ]; then
  echo "Syntax: $0 <experiment> <subject>"
  exit 1
fi;

EXPERIMENT=$1
SUBJECT=$2

pod_names | grep $EXPERIMENT | grep $SUBJECT | xargs echo | xargs kubectl delete pod --grace-period 0 --force
